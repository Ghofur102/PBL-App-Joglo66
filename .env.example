# ============================================
# ENV Configuration untuk Flutter App (Zami)
# ============================================

# Backend API Configuration
API_BASE_URL=http://10.28.239.114:8000

# API Token (generate via Laravel tinker)
# Command: 
#   php artisan tinker
#   $user = User::find(1);
#   $token = $user->createToken('API Token')->plainTextToken;
#   echo $token;
# 
# Copy token hasil generate ke sini:
API_TOKEN=your_api_token_here_generated_via_tinker
